package darpa.stac.graph_analyzer.graph.layout.adv;

import java.util.*;
import darpa.stac.graph_analyzer.graph.*;

class OptimizerModularity$1 implements Comparator<Edge> {
    public int compare(final Edge e1, final Edge e2) {
        e1.getClass();
        final double n = 0.0;
        e2.getClass();
        if (n == 0.0) {
            return 0;
        }
        e1.getClass();
        final double n2 = 0.0;
        e2.getClass();
        return (n2 < 0.0) ? 1 : -1;
    }
}